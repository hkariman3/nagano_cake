<h3>ジャンル編集</h3>

<%= form_with model: @genre, url:admin_genre_path, method: :PATCH do |f| %>

<div>
  ジャンル名
  <%= f.text_field :name %>
  <%= f.submit'変更を保存する', class:"btn-success" %>
  <% end %>
</div>